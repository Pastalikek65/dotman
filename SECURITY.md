# Security Policy

Supported: 0.1.x

Report via https://github.com/Pastalikek65/dotman/security/advisories/new — 48h response.

- Stores dotfiles in `~/.cache/dotman/repo` `0700` git repo, `0600` files
- No network by default, `git` only for local commits
